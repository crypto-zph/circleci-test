const Login         = require('./Login');
const Scheduler     = require('./Scheduler');
const FlashDeal     = require('./FlashDeal');
const Search        = require('./Search');
const Announcement  = require('./Announcement');
const Image         = require('./ImageClass');

module.exports = [
    Login,
    // Scheduler,
    // FlashDeal
    // Search
    Announcement,
    Image
];